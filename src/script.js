import { Event } from "./javaScript/event.js";


document.addEventListener('DOMContentLoaded',()=>{
    
    const event = new Event(
        [
            '.h1','.short-services','.ex','.start-with',
            '.project-infos','.one','.two','.services'
        ]
    );
})
